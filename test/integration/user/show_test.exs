defmodule MusicQuiz.UserShowTest do
  use MusicQuiz.ConnCase
  use Hound.Helpers

  import MusicQuiz.Factory
  import MusicQuiz.TestHelpers

  hound_session

  # test "displays the users email" do
    # navigate_to("/")
  # end
end
